[project]
name = "bao-cao-hien-truong-thuy-loi"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1",
  "google-genai>=1.0.0",
  "Pillow>=10.0.0",
  "reportlab>=4.0.0",
  "python-multipart>=0.0.9",
]

[tool.vercel]
entrypoint = "api.index:app"
